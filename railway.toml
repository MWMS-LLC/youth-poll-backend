[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Force deployment marker
[deploy.environment]
FORCE_DEPLOY = "2025-08-12-08:00"
DEPLOYMENT_VERSION = "OTHER_OPTION_FIX"
